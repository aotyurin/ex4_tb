

        // todo
        //  - configure bd
        //  - заполнить неизменяемыми полями в runtime - UserInfo, Cписок валют, Настройки валютных пар, открытых ордеров пользователя
        // - перед покупкой знать цену последней продажи, и наоборот, чтобы не уйти в минус на комиссии


        // todo task_1
                // --- открытый ордер
                // если день ордера меньше текущего дня или
                // процент отклонения цены от текущей больше 5
                    // отменить ордер
                    // выход
                // иначе
                    // если ордер на продажу
                        // если тренд растет
                            // если процент отклонения цены от текущей минимален
                                // отменить ордер
                                // если тренд продолжает расти
                                    // ждать бокового
                                    // проверить профит
                                    // запросить ордера на продажу и выставить ордер по цене из позиции -3 (или -2 в зависимости от продаваемого количества) от минимальной
                                // иначе (стал падать)
                                    // запросить цену
                                    // проверить профит
                                    // и выставить ордер по цене чуть меньшей
                            // жтать
                        // тренд падает
                            // ждать
                    // если ордер на покупку
                        // если тренд падает
                            // если процент отклонения цены от текущей минимален
                                // отменить ордер
                                // если тренд продолжает падать
                                    // ждать бокового
                                    // проверить профит
                                    // запросить ордера на покупку и выставить ордер по цене из позиции -3 (или -2 в зависимости от покупаемого количества) от минимальной
                                // иначе (стал расти)
                                    // запросить цену
                                    // проверить профит
                                    // и выставить ордер по цене чуть большей
                            // жтать
                        // тренд падает
                            // ждать