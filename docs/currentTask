

        // todo
        //  - configure bd
        //  - заполнить неизменяемыми полями в runtime - UserInfo, Cписок валют, Настройки валютных пар, открытых ордеров пользователя
        // - перед покупкой знать цену последней продажи, и наоборот, чтобы не уйти в минус на комиссии


        // todo task_1
        // List<UserOpenOrderDto> userOpenOrders = exFactory.getUserOpenOrders();
        // если ордер открыт
        // Задача - продать по максимуму
        // нужды данные при какой цене открыли ранее ордер, чтобы не продать в минус
        //          смотрим тренд
        //          если тренд растет
        //              ждем максимума
        //              если окрытый ордер не сработал и тренд начал падать -
        //                  считаем недополученную прибыль
        //                  если еще в плюсе
        //                      отменяем ордер
        //                      смотрим текущую цену
        //                      смотрим баланс пользователя
        //                      создаем новый ордер, но с ценой чуть меньше
        //                  иначе создаем 'наблюдателя'
        //          иначе создаем 'наблюдателя'
